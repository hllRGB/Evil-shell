/* vim: set ts=8 sw=8 nu rnu sts=8 et: */
#include "include/evilgeneral.h"

#include "main.h"

int evil_init_finished = 0;
struct user_str evil_user = {-1, -1, -1, -1, -1, -1, NULL, NULL, NULL};

int main(int argc, char * nullable * nonnull argv) {
        (void)argc;
        (void)argv;
        return EXIT_SUCCESS;
}
