你现在需要编辑termm.c.我正在尝试实现一个shell行编辑的终端管理实现。我打算维护：一个环形队列缓冲区，记录哪些字符是脏区需要redraw.然后，在内存中维护一个二维数组（即array[][]），用作行与列字符信息管理。我打算按照终端长宽组织，即该主二维数组中x和y索引分别对应终端被管理区域的行和列。现在我需要你这样实现：每个终端的字符位置需要接受以下内容:char [4]用于编码unicode。不使用wchar,直接字节编码.采用增量绘制策略，在环形队列中储存需增量绘制的信息，包括坐标和字符数据。每次要绘制时，计算偏移并打印文本。增量绘制所需要的结构体必须尽量紧凑，比如如果是一整行，那么只需要一个开头坐标即可，后续随着终端打印自动组织。光标偏移根据全局光标坐标和目标开头坐标差异计算。遇到SIGWINCH时，立即执行将所有字符设为脏排入队列，然后执行队列以清空终端屏幕中所管理的部分，并动态调整二维数组容量，并重新根据行状态重绘。这里我不需要你去读取行状态，我将在更高层自主实现，你仅需为我写：终端xy二维数组，屏幕上所呈现的字符结构体，脏数据重绘结构体，脏画面重绘逻辑即可。ANSI转义序列尽量集中爆发输出，比如不要打印一堆\e[A,而是使用\e[10A这种。使用c23标准，我拥有版本很新的clang/llvm.我目前正在termux下。请尽量指定-target aarch64-unknown-linux-android3.请帮我编写。
